# Deployment

Local development uses Docker Compose for PostgreSQL, Redis, FastAPI and React.

Production should add HTTPS, secret management, backups, monitoring, log aggregation and managed database/cache services.
