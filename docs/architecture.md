# Architecture

Frontend: presentation, navigation, filters, forms, charts, tables, and user actions.

Backend: persistence, business rules, calculations, analytics, AI orchestration, automation, permissions, and reporting.

PostgreSQL: system of record.
Redis: caching and background work.
External providers: isolated behind ingestion adapters.
