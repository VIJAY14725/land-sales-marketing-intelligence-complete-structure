# API

Version all endpoints under /api/v1.

Primary resources:
properties, comparables, competitors, leads, activities,
funnel, marketing, analytics, tasks, recommendations,
reports, automations, imports, auth, users.
